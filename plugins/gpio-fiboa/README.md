# gpio-fiboa

fiboa (Field Boundaries for Agriculture) plugin for gpio.

Adds `gpio fiboa` commands for improving and describing
GeoParquet files against the fiboa specification.

## Install

```bash
pip install gpio-fiboa
```

## Commands

- `gpio fiboa improve` — Add metrics, admin divisions, and schema metadata
- `gpio fiboa describe` — Show fiboa compliance info
